/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.6/userguide/java_library_plugin.html
 */

plugins {
    id 'java'
    id 'org.springframework.boot'
    id 'eclipse'
    id "io.spring.dependency-management" version "1.0.10.RELEASE"
    id "war"
}

war {
    archiveName = 'basic-gateway.war'
}
ext {
	set('springCloudVersion', "Hoxton.SR7")
}
   
repositories {
    mavenCentral()
    maven { url 'https://repo.spring.io/milestone' }
    maven { url 'https://repo.spring.io/libs-snapshot' }
    jcenter()
    maven { url 'http://oss.jfrog.org/artifactory/oss-snapshot-local/' }
}

sourceCompatibility = '1.8'

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

dependencies {
    compile ('org.springframework.cloud:spring-cloud-starter-gateway') {
	    exclude group: 'org.springframework.boot', module: 'spring-boot-starter-web'
	}
	compile ("io.projectreactor.tools:blockhound:1.0.4.RELEASE")
	compile("org.springframework.boot:spring-boot-starter-actuator")
	compile ('org.springframework.cloud:spring-cloud-starter-sleuth')
	compileOnly 'org.projectlombok:lombok:1.18.8'
	//needed while building from command line
	annotationProcessor 'org.projectlombok:lombok:1.18.8'
}

dependencyManagement {
	imports {
		mavenBom "org.springframework.cloud:spring-cloud-dependencies:${springCloudVersion}"
		mavenBom ("org.springframework.boot:spring-boot-gradle-plugin:2.3.3.RELEASE")
	}
}
